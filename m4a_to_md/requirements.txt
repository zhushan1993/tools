# iPhone录音转Markdown工具依赖
# Python依赖

# 音频处理
pydub>=0.25.1          # 音频文件格式转换和处理
SpeechRecognition>=3.10.0  # 语音识别核心库

# 可选：离线识别引擎（二选一）
vosk>=0.3.45         # 离线语音识别（需要下载语言模型）
# openai-whisper>=20231117  # OpenAI Whisper离线识别（模型较大）

# 音频I/O支持（根据需要安装）
# pyaudio>=0.2.11      # 音频录制和播放（需要系统依赖）

# 文本处理和工具
chardet>=5.2.0         # 字符编码检测
tqdm>=4.66.0           # 进度条显示
jieba>=0.42.1          # 中文分词（用于关键词提取）

# 系统依赖 (Ubuntu/Debian):
# sudo apt-get install ffmpeg libasound2-dev portaudio19-dev python3-pyaudio
#
# macOS:
# brew install ffmpeg portaudio
#
# Windows:
# 1. 下载安装 FFmpeg: https://ffmpeg.org/download.html
# 2. 添加到系统PATH
# 3. 对于pyaudio: pip install pipwin && pipwin install pyaudio

# 注意:
# 1. speech_recognition默认使用Google Web Speech API，需要网络连接
# 2. 如需离线识别，请安装vosk或whisper，并下载对应的中文语言模型
# 3. pyaudio在某些系统上可能需要额外配置，如非必要可不安装